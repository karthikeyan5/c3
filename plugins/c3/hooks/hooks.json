{
  "description": "C3 SessionStart hook — records the stable --resume session id (via c3-broker session-hook) so a resumed session auto-attaches to its last Telegram topic.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "c3-broker session-hook"
          }
        ]
      }
    ]
  }
}
