# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
*.egg-info/
build/
dist/

# Go
*.exe
*.dll
*.so
*.dylib
*.test
*.out
/bin/
/coverage.out
/c3-broker
/c3-claude-adapter
/c3-codex-adapter
/codex
/migrate-legacy

# Editors / OS
.DS_Store
*.swp
*.swo
*~
.vscode/
.idea/

# Internal review notes — not part of the public documentation set.
docs/.loop/

# Archived done-and-dusted docs — off the public tree, kept in git history.
junk/

# Local config + secrets (never commit these)
mappings.json
mappings.json.bak
*.env
.envrc

# Keep-out: operational docs carrying real infra values + internal run logs.
# NEVER commit (would leak keep-out infra/personal paths to the public repo).
docs/DEPLOY-telegram-proxy.md
MORNING-REVIEW-*.md
